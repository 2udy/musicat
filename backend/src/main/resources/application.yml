spring:
  datasource:
#    url: jdbc:mariadb://musicat.kr:3306/musicat?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    url: jdbc:mariadb://localhost:3306/musicat?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: root
    password: ssafy
#    password: ssafya702!
    driverClassName: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

  # security
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: dd7128c40ac72350cbbeb8f001764e04
            redirect-uri: "http://localhost:9999/api/login/oauth2/code/kakao" # http://localhost:8080/login/oauth2/code/kakao
            client-authentication-method: POST
            authorization-grant-type: authorization_code
            scope: profile_nickname, profile_image, account_email
            client-name: Kakao
        provider:
          kakao:
            authorization_uri: https://kauth.kakao.com/oauth/authorize
            token_uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user_name_attribute: id

server:
  port: 9999

# jwt secret key
jwt:
  header: Authorization
  secret: "%C*F-JaNdRgUkXp2s5v8y/A?D(G+KbPeShVmYq3t6w9z$C&E)H@McQfTjWnZr4u7"
  token-validity-in-seconds: 86400